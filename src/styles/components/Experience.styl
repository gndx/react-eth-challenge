@import "../_vars.css";
@import "../mixins.styl";
.Experience

    &-title
        title-base()
    &-container
        display: grid
        grid-template-columns:repeat(3,1fr)
    &-item
        color: var(--md-sys-color-on-surface)
        font-size: 16px

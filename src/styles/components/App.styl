body
  background: linear-gradient(#8da9c4 25%, white 45%) no-repeat;
  color: #FFF
  font-family: "Lucida Console", "Courier New", monospace;
  margin: 16px;

.Two-columns
  display: flex
  gap: 16px;
  & > *
    flex-grow: 1
@import "mixins.styl"

.Profile
  border-containers(profile)
  display flex
  flex-direction column
  align-items center
  justify-content center
  width: 100%
  margin-bottom: 20px
  gap 20px

.Profile-title
  font-size: 3rem
  font-weight: bold

.Profile-desc
  font-size: 2rem
  
  


import React from 'react';
import Card from './Card';

const Profile = () => {
  return (
    <Card>
      <h2 className="Profile-title">Profile</h2>
      <p className="Profile-desc">
        I am looking to consolidate myself as a Front-End programmer, and
        specialize in Javascript, ReactJs and NextJS, and now web3.
      </p>
    </Card>
  );
};

export default Profile;

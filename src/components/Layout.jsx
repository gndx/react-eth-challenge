import React from 'react';

const Layout = () => {
    return (
        <div>
            const {Academic: academic,
  address,
  avatar,
  email,
  experience,
  interest,
  languages,
  name,
  phone,
  profession,
  Profile: profile,
  skills,
  website,
}=useContext(AppContext)
        </div>
    );
};

export default ;
import React from "react";
import { Wrapper, Title } from "../../styles/components/GlobalStyle";

export const Profile = () => {
    return (
        <Wrapper>
            <Title>Profile</Title>
        </Wrapper>
    )
}